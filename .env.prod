# MetricFrame Production Configuration (test)
# Using default credentials for testing

# Frontend port (3000 is industry standard for web apps, non-privileged)
FRONTEND_PORT=3000

# CORS origins
CORS_ORIGINS=http://localhost,http://localhost:3000
