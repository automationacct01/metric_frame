# MetricFrame Backend Configuration
# Copy this file to .env and fill in your values

# Database (PostgreSQL for Docker, SQLite for Desktop)
DATABASE_URL=postgresql://metricframe:metricframe@localhost:5432/metricframe
# For SQLite (desktop app): DATABASE_URL=sqlite:///./metricframe.db

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# API Configuration
API_PREFIX=/api/v1

# ===========================================
# AI PROVIDER API KEYS
# ===========================================
# Users must configure their own API keys.
# Get keys from:
#   - Anthropic: https://console.anthropic.com/
#   - OpenAI: https://platform.openai.com/api-keys
#
# Leave blank if not using that provider.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ===========================================
# CREDENTIAL ENCRYPTION
# ===========================================
# Generate a new key for production:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# IMPORTANT: Each installation should generate its own unique key!

AI_CREDENTIALS_MASTER_KEY=

# ===========================================
# OPTIONAL: Additional AI Providers
# ===========================================
# These can be configured through the app's Settings page

# Together.ai
# TOGETHER_API_KEY=

# Azure OpenAI
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_DEPLOYMENT=

# AWS Bedrock
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=

# Local Models (Ollama, LM Studio, vLLM, etc.)
# In Docker, use http://host.docker.internal:11434/v1 to reach host machine
# LOCAL_AI_ENDPOINT=http://localhost:11434/v1

# ===========================================
# OPTIONAL: Web Search
# ===========================================
# DuckDuckGo is used by default (free, no key needed).
# For higher quality results, configure a Tavily API key:
# TAVILY_API_KEY=
